# 4.1
# Programmet vil kjøre frem til linje 4, der det vil komme en error når det prøves å legge til en streng og en integer.
# 4.2
# Hvis bruker gir input som gjør if spørresetningen sann (b < 10) vil programmet kræsje. 
# Programmet sløser også minne på å lage en ny variabel for inputet.
# Dette kan fikses ved å ta "if int(a) < 10: print(a, "Hei!)" og fjerne b variabelen. Eller å bruke a i stedet for b i print linjen.